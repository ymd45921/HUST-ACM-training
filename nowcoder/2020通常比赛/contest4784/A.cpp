#include <iostream>
#include <cstdio>

using namespace std;
typedef long long longs;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t,n,m,a,b;
    cin>>t;
    while (t--)
    {
        
    }


    return 0;
}